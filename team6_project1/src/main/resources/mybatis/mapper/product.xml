<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mycompany.webapp.dao.ProductDao">
	<select id="selectByPno" parameterType="String" resultType="product">
		select * from product where pid=#{pid}
	</select>
	
	<select id="selectAll" resultType="product">
		select * from product
	</select>
	
	<select id="selectByPname" resultType="product">
		select * from product where pname=#{pname}
	</select>
	
	<update id="updateStock" parameterType="product">
		update product set pquantity=#{pquantity} where pid=#{pid}
	</update>
</mapper>