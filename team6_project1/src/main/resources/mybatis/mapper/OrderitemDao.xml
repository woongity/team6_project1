<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mycompany.webapp.dao.OrderitemDao">
	<select id="selectOrderitemJoinProductByMid" parameterType="String" resultType="com.mycompany.webapp.dto.OrderitemJoinProductJoinOrder">
		select p.pname, o.pquantity, p.pimage1, p.pbrand, p.pcolorimage, p.pcolor, p.psize, p.pcode, o.oid ,o.pquantity*p.pprice as oprice,ord.ostatus, ord.otime
		from 
			orderitem o
		inner join
			product p
        on o.pcode = p.pcode and o.pcolor=p.pcolor and o.psize = p.psize
        inner join 
            orderinfo ord
        on o.oid = ord.oid
        where ord.mid = #{mid}
        order by ord.otime desc
	</select>
  <select id="selectOrderitemJoinProductByOid" parameterType="String" resultType="com.mycompany.webapp.dto.OrderitemJoinProduct">
		select p.pname, o.pquantity, p.pimage1, p.pbrand, p.pcolor, p.psize, p.pcode, o.oid ,o.pquantity*p.pprice as oprice
		from 
			orderitem o
		inner join
			product p
		on o.pcode = p.pcode and o.pcolor =p.pcolor and o.psize=p.psize
	</select>
</mapper>